{
  "name": "install-gh-cli-action",
  "version": "0.1.0",
  "description": "Download, cache and install the GH CLI to the workspace (GitHub Action)",
  "main": "run.js",
  "scripts": {
    "build": "ncc build src/run.js -o dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/arimal199-org/install-gh-cli-action.git"
  },
  "keywords": [
    "github",
    "actions"
  ],
  "author": "arimal199-org",
  "bugs": {
    "url": "https://github.com/arimal199-org/install-gh-cli-action/issues"
  },
  "homepage": "https://github.com/arimal199-org/install-gh-cli-action#readme",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/tool-cache": "^2.0.1"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.0"
  }
}
